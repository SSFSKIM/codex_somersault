import type { Message } from '../../types/message.js'

export const projectView: any = (messages: Message[]) => messages
export const applyContextCollapse: any = async (messages: Message[]) => messages
export const resetOperations: any = () => {}
