# Phase 9.6c Fix Log — Spec 42 (misc / long-tail catch-all)

**Date applied:** 2026-05-10
**Source review:** `docs/specs/PHASE9-ADVERSARIAL-42.md` (Phase 9.5b skeptic review)
**Triggered by:** Phase 9.7 Agent E spec 35 fix added "OUT-of-scope: SandboxManager / `/sandbox-toggle` / `src/main.tsx:201,314-316` deferred to spec 42 with §11 cross-ref" — but spec 42 had no corresponding owner block. This retry closes the loop.

## Severity vs fixes applied

| Severity | Adversarial finding | Fix applied | Spec 42 location |
|---|---|---|---|
| CRITICAL | #1 Sandbox subsystem unowned anywhere | Added new **§X "Sandbox subsystem (delegated from 35)"** with file table, boot wiring (`src/main.tsx:201,314-316`), `dangerouslySkipPermissions` distinction, and `bash/ast.ts` distinction | §X.1–§X.5 (new) + §1 IN-scope bullet |
| HIGH | #2 §1 IN-scope omits 23% co-ownership | Added co-ownership architectural sub-table with bash/shell/powershell (28), swarm (21), computerUse (16), model (17). Added 4 OUT-of-scope rows (09/10, 14/30, 16, 22). | §1 OUT-of-scope table (4 rows) + new "Co-ownership at architectural level" sub-table |
| HIGH | #3 `bash/ast.ts` shell trust-boundary invisible | Added explicit "Shell trust-boundary" paragraph quoting file-header verbatim ("FAIL-CLOSED… NOT a sandbox… 'Can we produce a trustworthy argv[]'"). Cross-linked to spec 09/10. Added §X.4 distinction from sandbox-runtime. | §1 co-ownership sub-table + §X.4 |
| MEDIUM | #4 `ansiToPng.ts` (215KB) embedded asset, not source | Added explicit row in source-coverage inventory: "embedded asset, not source — bundled 24×48 Fira Code bitmap-font rasterization regenerated by `scripts/generate-bitmap-font.ts`". | §1 source-coverage inventory |
| MEDIUM | #5 §A `errorIds.ts` description wrong | Replaced "obfuscated trace IDs" with "descriptive trace-ID identifiers… Names are fully human-readable". Added line cites `src/constants/errorIds.ts:15` (E_TOOL_USE_SUMMARY_GENERATION_FAILED), `:10-12` (assignment protocol), `:12` (Next ID: 346 tracker). | §A `errorIds.ts` row |
| LOW | #6 `src/utils/` count drift (329 vs 327) | Reconciled: "327 source files; `ls` returns 329 = 327 + `.DS_Store` + `specs/` subdir entry". Reconciled with 42a §0. | §1 source-coverage inventory |

## Verification (verify-before-edit)

| Claim | Verification command / Read | Confirmed |
|---|---|---|
| `src/utils/sandbox/` contains only `sandbox-adapter.ts` and `sandbox-ui-utils.ts` | `ls src/utils/sandbox/` | ✓ |
| `src/main.tsx:201` is `import { SandboxManager } from './utils/sandbox/sandbox-adapter.js';` | `grep -n 'sandbox\|Sandbox' src/main.tsx` | ✓ |
| `src/main.tsx:314-316` are the three telemetry booleans | grep above | ✓ |
| `src/commands.ts:149` registers `sandboxToggle` | `grep -n 'sandbox' src/commands.ts` | ✓ (also exported at line 336) |
| `src/utils/sandbox/sandbox-adapter.ts` wraps `@anthropic-ai/sandbox-runtime`'s `SandboxManager` | Read header (30 lines) | ✓ ("Adapter layer that wraps `@anthropic-ai/sandbox-runtime`…") |
| File line counts (985, 12, 50, 82) | `wc -l` on all four sandbox files | ✓ |
| `src/utils/bash/ast.ts` is 2,679 lines | `wc -l` | ✓ |
| `bash/ast.ts` header says "FAIL-CLOSED… This is NOT a sandbox." | `head -20` | ✓ |
| `src/utils/ansiToPng.ts` is bitmap-PNG renderer (not source LOC) | `head -5` | ✓ ("Render ANSI-escaped terminal text directly to a PNG image. Replaces the previous ansiToSvg → @resvg/resvg-wasm pipeline.") |
| `errorIds.ts:15` has `E_TOOL_USE_SUMMARY_GENERATION_FAILED = 344` | `grep -n` | ✓ |
| `errorIds.ts:12` has `Next ID: 346` | `grep -n` | ✓ (in header comment block lines 10–12) |
| `errorIds.ts` total line count (15 lines) — file is tiny because each `const` is exported individually for DCE | `wc -l` | ✓ |
| Spec 35 cross-refs at `:18` and `:623` exist and delegate here | `grep -n 'sandbox\|42' docs/specs/35-mode-remote-server.md` | ✓ (file is named `35-mode-remote-server.md`, not `35-remote-sessions.md` as adversarial review wrote) |
| `src/utils/` ls returns 329 entries (incl. `.DS_Store` + `specs/` subdir) | `ls src/utils/ \| wc -l` | ✓ |

## Cross-spec ripple

- **Spec 35 (`35-mode-remote-server.md:18`, `:623`)**: cross-ref now resolves. No change required to spec 35 — its text already correctly cites "→ 42" and "Cross-references include … 42 for sandbox semantics". Spec 42 §X.5 now reciprocates. ✓
- **Spec 09 / 10 (permissions)**: §1 OUT-of-scope adds `bash/`+`shell/`+`powershell/` (28 files) and the architectural-level "shell trust-boundary" paragraph delegates `bash/ast.ts` to 09/10. No new asks from 42 to 09/10 — purely citation. (Future spec 09/10 revise can pick this up if desired.)
- **Spec 14 / 30 (coordinator)**: §1 OUT-of-scope adds `utils/swarm/` (21 files). Resolves the asymmetry the reviewer flagged ("§1 OUT-of-scope cites 30 for `services/AgentSummary/` etc. but not for `utils/swarm/`"). ✓
- **Spec 16 (tool-mcp-lsp)**: §1 OUT-of-scope adds `utils/computerUse/` (16 files). ✓
- **Spec 22 (api)**: §1 OUT-of-scope adds `utils/model/` (17 files). ✓
- **Spec 42a**: counts now reconcile (327 source files; spec 42 explains the 329-vs-327 delta).

## Post-fix verdict

**APPROVED.** All 1 CRITICAL + 2 HIGH + 2 MEDIUM + 1 LOW finding from `PHASE9-ADVERSARIAL-42.md` are addressed. New §X is the architectural owner block for the sandbox subsystem; spec 35's deferral now lands cleanly. The catch-all spec retains its "coverage receipt" character — no design critique introduced, all behavioral claims carry `src/<path>:<line-range>` cites, all verbatim assets in §6 untouched.

## Items intentionally NOT changed

- §6 verbatim assets — no sandbox source-code blocks added inline because `sandbox-adapter.ts` is 985 lines (over inline budget) and the file's interesting surface is its **delegation** to `@anthropic-ai/sandbox-runtime` (out-of-leak vendor package), not the wrapper's internals. §X.1 names every export and the file header is quoted; that is the architectural surface.
- §12 (open questions / Phase 9 escalations) — already swept by Phase 9.7; no new residuals introduced by this fix.
- §A appendix structure — only `errorIds.ts` row tightened; other six rows unchanged.
- Hardest-to-verify claim from adversarial review (buddy SALT security implication) — not addressed in this retry; adversarial review correctly flagged it as "Flag for future revise" and it is non-blocking.
